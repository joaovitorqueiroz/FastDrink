import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginTop: 10,
        marginHorizontal: 20,
        marginBottom: 30,
    },
    rowIngredients: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    measureText: {
        marginLeft: 35,
        fontSize: 16,
    },
    ingredientText: {
        marginLeft: 5,
        fontSize: 16,
    },
});
