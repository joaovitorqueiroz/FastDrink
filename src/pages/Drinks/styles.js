import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 20,
    },
    notFoundContainer: {},
    notFoundText: {
        fontSize: 16,
        color: '#333',
    },
});
