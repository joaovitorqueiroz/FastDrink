import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    titleContainer: {
        marginLeft: 30,
        marginBottom: 20,
        flexDirection: 'row',
    },
    titleText: {
        marginLeft: 20,
        fontSize: 20,
    },
    flatListContainer: {
        alignItems: 'center',
    },
});
